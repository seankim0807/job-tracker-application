# Backend

Flask backend for Job Application Tracker. Creates `jobs.db` in the backend folder and exposes API endpoints on port 5000.

Install and run:

```bash
python -m pip install -r requirements.txt
python app.py
```
